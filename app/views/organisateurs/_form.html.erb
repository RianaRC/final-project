<%= form_with(model: organisateur, local: true) do |form| %>
  <% if organisateur.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(organisateur.errors.count, "error") %> prohibited this organisateur from being saved:</h2>

      <ul>
      <% organisateur.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :kind %>
    <%= form.text_field :kind %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :about %>
    <%= form.text_area :about %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
